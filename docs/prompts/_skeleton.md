# Phase <n> - <name>

> Copy this file to `phase-<n>-<slug>.md` and fill it in.
> The contract these sections come from is in `README.md`; read it first.
> Delete these three lines when you copy.

## Role

<What the agent is acting as. A stance that changes behaviour, not a job title.>

## Input

<Which artifacts this phase reads, by path. Name any contract with another phase,
since a reader of this file cannot see the other end. Say what it must not read.>

## This phase is not

<The prohibition list. Named artifacts, not attitudes. Include the adjacent
temptation, not only the distant one - the rule that gets violated is the one
next door to the real job.>

- <not this>
- <not this either>

## Method

<How the work is done. Fan-out and on what axes. Whether agents stay isolated
until finished. Any loop states every one of its termination conditions here.>

## Deliverable

<The artifact: path, required sections, and any structured output a later phase
consumes. If another phase reads part of this as data, its format belongs here.>

- path: `<...>`
- sections: <...>

## Exit condition

<What must be true to hand off, and what the maintainer is being asked to
approve. Name the decision so the gate is a decision and not a nod.>

---

## Worked example: <domain>

> Domain-specific material lives here and nowhere else in the file.
> The body above must work for a domain nobody has thought about yet.

<...>
